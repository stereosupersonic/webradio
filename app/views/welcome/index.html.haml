.row.card-deck
  - @stations.each do |station|
    %div{ id: dom_id(station), class: "col-3 m-3 d-flex align-items-stretch" }
      .card.mt-1{ style: "width: 30rem;"}
        .card-header=station.name
        .card-body
          = link_to image_tag(station.station_logo, class: "rounded mx-auto d-block img-fluid"), station
        .card-footer
          .d-grid.gap-2
            = link_to "PLAY", station, class: "btn btn-warning btn-lg"
